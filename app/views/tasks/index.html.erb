<div class="container">
<h1>My tasks</h1>
<ul style="padding-left: 0;">
  <% @tasks.each do |task| %>
    <% if task.completed %>
      <input type="checkbox" id="task" checked>
      <label for="task"><%= link_to task.title, task_path(task) %> <%= link_to '🛢', task_path(task.id), method: :delete, data: {confirm: 'Are you sure you want to delete?'} %></label><br>
    <% else %>
      <input type="checkbox" id="task">
      <label for="task"><%= link_to task.title, task_path(task) %> <%= link_to '🛢', task_path(task), method: :delete, data: {confirm: 'Are you sure you want to delete?'} %></label><br>
    <% end %>
  <% end %>
</ul>
 <%= link_to '+ Add new task', new_task_path(@task) %>
</div>
